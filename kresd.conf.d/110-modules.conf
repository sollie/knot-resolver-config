-- Load modules

modules.load('hints > iterate') -- Hints AFTER iterate
modules.load('policy > hints')  -- Policy AFTER hints
modules.load('view < cache')    -- View BEFORE cache
modules.load('stats')
modules.load('predict')
modules.load('prefill')
modules.load('http')
http.config({
	host = '127.0.0.1',
	port = 8453,
	key = '/etc/letsencrypt/live/' .. fqdn .. '/privkey.pem',
	cert = '/etc/letsencrypt/live/' .. fqdn .. '/fullchain.pem',
	geoip = '/var/lib/knot-resolver/geoip.mmdb'
})
